# Azure DevOps Configuration
# Copy this file to .ado.env and fill in your values

# Your Azure DevOps organization URL
# Example: https://dev.azure.com/contoso
ADO_ORG_URL=https://dev.azure.com/YOUR_ORG_NAME

# Personal Access Token with the following scopes:
# - Agent Pools (Read & Manage)
# - Service Connections (Read, Query & Manage)
# - Build (Read & Execute)
# - Code (Read)
# - Project and Team (Read, Write & Manage)
# Create at: https://dev.azure.com/YOUR_ORG_NAME/_usersSettings/tokens
ADO_PAT=YOUR_PERSONAL_ACCESS_TOKEN_HERE

# Azure DevOps Project name (will be created if doesn't exist)
ADO_PROJECT=ADO-DNS-Lab

# Agent pool name (will be created if doesn't exist)
ADO_POOL=DNS-Lab-Pool

# Azure tenant domain
# Example: contoso.onmicrosoft.com
AZURE_TENANT=yourtenant.onmicrosoft.com
